import FormItem from '../../../../FormItem/FormItem';

const ScheduleFormStartDateInput: React.FC = () => {
  return <FormItem text="Open date picker" onClick={() => {}} />;
};

export default ScheduleFormStartDateInput;
